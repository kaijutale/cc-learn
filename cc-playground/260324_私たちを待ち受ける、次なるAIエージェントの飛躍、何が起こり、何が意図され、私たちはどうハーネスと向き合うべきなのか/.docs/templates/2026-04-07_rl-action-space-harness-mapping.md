機能名: 強化学習の概念体系とハーネス設計のマッピング

- セッション名: 行動空間の制限 — 強化学習からハーネスへの概念接続
- 日付: 2026-04-07 13:20:43
- 概要: note記事の「行動空間の制限」という表現を起点に、強化学習(RL)の概念体系（状態空間・行動空間・報酬）とAIエージェントのハーネス設計の対応関係を議論・整理した。かもねの初期仮説「状態空間=ハーネス構築」を修正し、ハーネスは3概念全体を包含する上位概念であることを確認した。
- 実装内容:
  - **記事の「行動空間の制限」解説**: 柱1「関心ごとの分離」内の概念。各エージェントに「やらないこと」を定義し、注意力を専門領域に集中させる仕組み。収束値をはがす3アプローチの2番目「制約ルールの定義」と直結
  - **「行動空間」の語源**: 強化学習(Reinforcement Learning)の専門用語。エージェントが取りうる全ての行動の集合を多次元空間として捉える概念
  - **強化学習の位置づけ**: 機械学習の3大パラダイム（教師あり・教師なし・強化学習）の1つ。LLM自身もRLHF（人間のフィードバックによる強化学習）で訓練されている
  - **RL → ハーネス設計のマッピング確定**:
    - 状態空間 = エージェントが観測できるコンテキスト（CLAUDE.md、.docs/specs、コードベース）
    - 行動空間 = エージェントが取りうるアクションの範囲（ツール、制約ルール）
    - 報酬 = 検証ツールからのフィードバック（テスト結果、lint、スクリーンショット比較）
    - ハーネス = 上記3つ全体を設計する構造的な枠組み
  - **記事の3アプローチとの対応**:
    - 専門知識の注入 → 状態空間の設計
    - 制約ルールの定義 → 行動空間の制限
    - 検証ツールとの接続 → 報酬の設計
- 設計意図:
  - 記事で使われている「行動空間」が強化学習由来であることを明示し、単なる比喩ではなく構造的に正確な概念であることを確認した
  - かもねの「状態空間=ハーネス構築」という仮説を検証・修正。ハーネスは状態空間だけでなく3概念全体を設計する上位概念。この修正により、ハーネス設計の全体像がRL的フレームワークで一貫して説明できるようになった
- 副作用:
  - なし（概念整理のみ、コード変更なし）
- 関連ファイル:
  - `.docs/references/pdf/next-ai-agent-leap-and-harness.pdf` — 元記事（「行動空間の制限」の出典）

## 議論の経緯

1. **起点**: かもねが「行動空間の制限とは？」と質問
2. **語源の深掘り**: 「なぜ行動空間と呼ぶのか」→ 強化学習の用語であることを確認
3. **強化学習の位置づけ**: 「AI領域の用語？」→ ML 3大パラダイムの1つ、LLMのRLHFにも使われている
4. **かもねの仮説**: 「状態空間=ハーネス構築、行動空間=Subagentの制限対象」と提案
5. **仮説の修正**: 行動空間の対応はOK。状態空間=ハーネスは範囲が広すぎる。ハーネスは状態空間・行動空間・報酬の全てを包含する上位概念

## 学び

- **記事の用語は正確**: 「行動空間の制限」は強化学習の概念をそのまま適用しており、曖昧な比喩ではない。筆者が意図的にRL的フレームワークでエージェント設計を説明している
- **ハーネス設計 = RL環境設計**: 強化学習で「良いエージェント」より「良い環境」が重要とされるのと同様、記事も「モデルの性能ではなく構造の話」と繰り返している
- **かもねのCLAUDE.mdは既に3概念を網羅**: 状態空間（Secrets 3層、Stack指定）、行動空間（Prohibition、Agent Protocol）、報酬（Build/Testの検証ルール）が含まれている
