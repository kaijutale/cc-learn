---
date: 2026-05-03
audit_skill: coordination-harness-integrity-fork
audit_target: "~/.claude/skills/ (coordination subset, 13 skills) + ~/.claude/agents/ (coordination subset, 12 agents)"
verdict: NO-GO
machine_decision: true
critical_count: 31
major_count: 12
minor_count: 0
zsh_glob_violation: true
---

# Coordination Harness Integrity Audit — 2026-05-03

## Verdict (機械決定論)

```yaml
verdict: NO-GO
critical_count: 31
major_count: 12
minor_count: 0
zsh_glob_violation: true
ruleset_results:
  A_frontmatter: pass
  B_tools_wiring: pass
  C_bang_syntax: fail   # C-3 違反 31 件
  D_observability: fail # D-1 違反 12 件 (6 skill × 2 keys)
  E_prompt_template: pass
verdict_logic:
  critical_threshold: 1
  zsh_glob_no_go: true
  major_threshold: null
machine_decision: true
last_validated_at: 2026-05-03
```

verdict 決定経路: **Verdict logic step 7** (`C-3 violation present → verdict = "NO-GO"` zsh null_glob hang 致命傷格上げトリガー) によりNO-GO確定。

---

## Self-Eating Dogfood (本skill自身の検証結果)

本skill (`coordination-harness-integrity-fork`) 自身も検証対象に含めた self-check を実施。結果は **すべての ruleset 条件を pass**:

- A-1 OK: `context: fork` + `subagent: team-reviewer` 両方 frontmatter で宣言
- A-2 OK: subagent 指定 `team-reviewer` は `~/.claude/agents/team-reviewer.md` に実在
- A-3 OK: team-reviewer の `tools:` (Read/Grep/Glob/Bash) と本文「Edit/Write 非装備」記述に矛盾なし
- A-4 N/A: `disable-model-invocation` 不付与、description 末尾「明示呼出専用」明記
- B-1 OK: 関連 agent の tools と本文の整合性問題なし
- B-2 OK: coder agent (本skill責務外だが連携先) は `Skill` を tools: に持つ
- B-3 OK: team-reviewer は `Skill` を持たない (verifier 専念で構造保証)
- C-1 OK: !構文行は単純コマンド (date/pwd/ls/find/head) のみ
- C-2 OK: `$()` / `${}` / `{a,b,c}` 不混入
- C-3 OK: `find` 全行に `-maxdepth 1` または `-maxdepth 2` 指定済 (lines 46/50/51/52)
- C-4 OK: `find -exec` / `git --no-pager` 不使用
- D-1 OK: `tool_uses_count` / `file_writes_count` / `duration_sec` 3 keys present (lines 187-194)
- D-2 OK: duration 計測手順 (`date +%s` 開始/終了差分) 明記 (line 198)
- E-1 OK: `.docs/specs/CURRENT/` 参照 (検証目的の reference path 言及のみ)
- E-2 OK: 出力先 `.docs/coordination-integrity/YYYY-MM-DD.md` (本skill配下正規カテゴリ)
- E-3 OK: Step 番号重複なし (本skillは Step 番号を持たない設計)

**本skill自身の verdict は GO** (初期動作確認の必要条件達成、self-eating dogfood成立)。本skillが自分自身に対しては妥当な GO Verdict を出せている。

---

## Issues (severity 別、合計 43 件)

### 🔴 Critical (31 件) — Ruleset C-3 違反 (zsh null_glob hang 回避)

判定基準: `find` には `-maxdepth N` 指定がある (zsh null_glob hang 回避、過去bug 2026-04-19) → **NO-GO格上げトリガー**

過去 bug 検証ログ (`.docs/knowledge/skill-bang-syntax-glob-failure/2026-05-01-zsh-nullglob-result.md`) と本skillの ruleset 文言にギャップあり: 過去bug の **実際の発火条件**は `ls glob` (例: `ls openapi.*`) で、`find -name` パターンは literal 引数で渡されるため shell glob 展開を経由せず安全と実証されている。しかし ruleset C-3 文言は「`find` に `-maxdepth N` 指定」を厳密に要求している。team-reviewer は ruleset 文言を緩めず機械的に適用する (rubric 遵守)。

| # | severity | rule_id | location | line | evidence | description |
|---|---|---|---|---|---|---|
| 1 | critical | C-3 | ~/.claude/skills/red-test-fork/SKILL.md | 35 | `!\`find src -type f -name "*.ts"\`` | `-maxdepth N` 指定なし |
| 2 | critical | C-3 | ~/.claude/skills/red-test-fork/SKILL.md | 36 | `!\`find src -type f -name "*.tsx"\`` | `-maxdepth N` 指定なし |
| 3 | critical | C-3 | ~/.claude/skills/red-test-fork/SKILL.md | 37 | `!\`find src -type f -name "*.js"\`` | `-maxdepth N` 指定なし |
| 4 | critical | C-3 | ~/.claude/skills/red-test-fork/SKILL.md | 38 | `!\`find src -type f -name "*.jsx"\`` | `-maxdepth N` 指定なし |
| 5 | critical | C-3 | ~/.claude/skills/red-test-fork/SKILL.md | 41 | `!\`find src -type f -name "*.test.ts"\`` | `-maxdepth N` 指定なし |
| 6 | critical | C-3 | ~/.claude/skills/red-test-fork/SKILL.md | 42 | `!\`find src -type f -name "*.test.tsx"\`` | `-maxdepth N` 指定なし |
| 7 | critical | C-3 | ~/.claude/skills/red-test-fork/SKILL.md | 43 | `!\`find src -type f -name "*.spec.ts"\`` | `-maxdepth N` 指定なし |
| 8 | critical | C-3 | ~/.claude/skills/red-test-fork/SKILL.md | 44 | `!\`find src -type f -name "*.spec.tsx"\`` | `-maxdepth N` 指定なし |
| 9 | critical | C-3 | ~/.claude/skills/implement-fork/SKILL.md | 35 | `!\`find src -type f -name "*.test.ts"\`` | `-maxdepth N` 指定なし |
| 10 | critical | C-3 | ~/.claude/skills/implement-fork/SKILL.md | 36 | `!\`find src -type f -name "*.test.tsx"\`` | `-maxdepth N` 指定なし |
| 11 | critical | C-3 | ~/.claude/skills/implement-fork/SKILL.md | 37 | `!\`find src -type f -name "*.spec.ts"\`` | `-maxdepth N` 指定なし |
| 12 | critical | C-3 | ~/.claude/skills/implement-fork/SKILL.md | 38 | `!\`find src -type f -name "*.spec.tsx"\`` | `-maxdepth N` 指定なし |
| 13 | critical | C-3 | ~/.claude/skills/implement-fork/SKILL.md | 41 | `!\`find src -type f -name "*.ts"\`` | `-maxdepth N` 指定なし |
| 14 | critical | C-3 | ~/.claude/skills/implement-fork/SKILL.md | 42 | `!\`find src -type f -name "*.tsx"\`` | `-maxdepth N` 指定なし |
| 15 | critical | C-3 | ~/.claude/skills/implement-fork/SKILL.md | 43 | `!\`find src -type f -name "*.js"\`` | `-maxdepth N` 指定なし |
| 16 | critical | C-3 | ~/.claude/skills/implement-fork/SKILL.md | 44 | `!\`find src -type f -name "*.jsx"\`` | `-maxdepth N` 指定なし |
| 17 | critical | C-3 | ~/.claude/skills/verify-test-fork/SKILL.md | 34 | `!\`find src -type f -name "*.ts"\`` | `-maxdepth N` 指定なし |
| 18 | critical | C-3 | ~/.claude/skills/verify-test-fork/SKILL.md | 35 | `!\`find src -type f -name "*.tsx"\`` | `-maxdepth N` 指定なし |
| 19 | critical | C-3 | ~/.claude/skills/verify-test-fork/SKILL.md | 36 | `!\`find src -type f -name "*.js"\`` | `-maxdepth N` 指定なし |
| 20 | critical | C-3 | ~/.claude/skills/verify-test-fork/SKILL.md | 37 | `!\`find src -type f -name "*.jsx"\`` | `-maxdepth N` 指定なし |
| 21 | critical | C-3 | ~/.claude/skills/verify-test-fork/SKILL.md | 40 | `!\`find src -type f -name "*.test.ts"\`` | `-maxdepth N` 指定なし |
| 22 | critical | C-3 | ~/.claude/skills/verify-test-fork/SKILL.md | 41 | `!\`find src -type f -name "*.test.tsx"\`` | `-maxdepth N` 指定なし |
| 23 | critical | C-3 | ~/.claude/skills/verify-test-fork/SKILL.md | 42 | `!\`find src -type f -name "*.spec.ts"\`` | `-maxdepth N` 指定なし |
| 24 | critical | C-3 | ~/.claude/skills/verify-test-fork/SKILL.md | 43 | `!\`find src -type f -name "*.spec.tsx"\`` | `-maxdepth N` 指定なし |
| 25 | critical | C-3 | ~/.claude/skills/llm-debate-implementer/SKILL.md | 36 | `!\`find src -type f \( -name "*.ts" -o ... \) ... \|\| echo "(no source yet)"\`` | `-maxdepth N` 指定なし |
| 26 | critical | C-3 | ~/.claude/skills/llm-debate-tester/SKILL.md | 35 | `!\`find .docs/specs -type f -name "*.md" 2>/dev/null \| head -30 \|\| echo "(no specs)"\`` | `-maxdepth N` 指定なし |
| 27 | critical | C-3 | ~/.claude/skills/llm-debate-tester/SKILL.md | 38 | `!\`find . -type f \( -name "*.test.ts" -o ... \) -not -path "./node_modules/*" ...\`` | `-maxdepth N` 指定なし |
| 28 | critical | C-3 | ~/.claude/skills/llm-debate-documenter/SKILL.md | 38 | `!\`find .docs -type f -name "*.md" 2>/dev/null \| head -30 \|\| echo "(no .docs)"\`` | `-maxdepth N` 指定なし |
| 29 | critical | C-3 | ~/.claude/skills/llm-debate-ui-designer/SKILL.md | 35 | `!\`find .docs/designs -type f -name "*.md" 2>/dev/null \| head -20 \|\| echo "(no .docs/designs)"\`` | `-maxdepth N` 指定なし |
| 30 | critical | C-3 | ~/.claude/skills/llm-debate-ui-designer/SKILL.md | 38 | `!\`find src -type d \( -name "components" -o -name "ui" \) 2>/dev/null \| head -10 \|\| echo "(no UI components dir)"\`` | `-maxdepth N` 指定なし |
| 31 | critical | C-3 | ~/.claude/skills/llm-debate-ui-designer/SKILL.md | 41 | `!\`find .docs/designs -name "aesthetic.md" 2>/dev/null \| head -10 \|\| echo "(no aesthetic.md yet)"\`` | `-maxdepth N` 指定なし |

**zsh_glob_violation: true** (C-3 専用 NO-GO 格上げトリガー、Verdict logic step 7 が発火)

### 🟠 Major (12 件) — Ruleset D-1 違反 (Observability schema 不一致)

判定基準: 各 fork skill (red/implement/verify-test-fork, llm-debate*, 本skill) の出力フォーマット内 `Observability` セクションに `tool_uses_count`, `file_writes_count`, `duration_sec` 3キー present (severity major、NO-GO格上げトリガーではない)

llm-debate 系 6 skill では `Observability` セクションが `duration_sec` + `files_read` の **2 キー**のみで構成され、`tool_uses_count` と `file_writes_count` が欠落している。red/implement/verify-test-fork は 3 キー全て present。

| # | severity | rule_id | location | line | evidence | description |
|---|---|---|---|---|---|---|
| 32 | major | D-1 | ~/.claude/skills/llm-debate/SKILL.md | 138-141 | Observability section uses `duration_sec` only | `tool_uses_count` MISSING |
| 33 | major | D-1 | ~/.claude/skills/llm-debate/SKILL.md | 138-141 | Observability section uses `duration_sec` only | `file_writes_count` MISSING |
| 34 | major | D-1 | ~/.claude/skills/llm-debate-implementer/SKILL.md | 113-115 | `Observability:\n  duration_sec: <N>\n  files_read: <N>` | `tool_uses_count` MISSING |
| 35 | major | D-1 | ~/.claude/skills/llm-debate-implementer/SKILL.md | 113-115 | `Observability:\n  duration_sec: <N>\n  files_read: <N>` | `file_writes_count` MISSING |
| 36 | major | D-1 | ~/.claude/skills/llm-debate-tester/SKILL.md | 112-114 | `Observability:\n  duration_sec: <N>\n  files_read: <N>` | `tool_uses_count` MISSING |
| 37 | major | D-1 | ~/.claude/skills/llm-debate-tester/SKILL.md | 112-114 | `Observability:\n  duration_sec: <N>\n  files_read: <N>` | `file_writes_count` MISSING |
| 38 | major | D-1 | ~/.claude/skills/llm-debate-reviewer/SKILL.md | 135-137 | `Observability:\n  duration_sec: <N>\n  files_read: <N>` | `tool_uses_count` MISSING |
| 39 | major | D-1 | ~/.claude/skills/llm-debate-reviewer/SKILL.md | 135-137 | `Observability:\n  duration_sec: <N>\n  files_read: <N>` | `file_writes_count` MISSING |
| 40 | major | D-1 | ~/.claude/skills/llm-debate-documenter/SKILL.md | 127-129 | `Observability:\n  duration_sec: <N>\n  files_read: <N>` | `tool_uses_count` MISSING |
| 41 | major | D-1 | ~/.claude/skills/llm-debate-documenter/SKILL.md | 127-129 | `Observability:\n  duration_sec: <N>\n  files_read: <N>` | `file_writes_count` MISSING |
| 42 | major | D-1 | ~/.claude/skills/llm-debate-ui-designer/SKILL.md | 122-124 | `Observability:\n  duration_sec: <N>\n  files_read: <N>` | `tool_uses_count` MISSING |
| 43 | major | D-1 | ~/.claude/skills/llm-debate-ui-designer/SKILL.md | 122-124 | `Observability:\n  duration_sec: <N>\n  files_read: <N>` | `file_writes_count` MISSING |

---

## 反インフレチェック (judging-review-severity プリロード適用)

- **「問題ゼロは疑う」**: 12 skill + 12 agent で issues=0 だったか? **NO** — 43 件検出 (能動的検出成立)
- **「動く ≠ 最善」**: C-3 違反は実動作で hang 発生していないが ruleset 違反として報告 (rubric 遵守)
- **「全 ruleset 合格は珍重、ただし合格判定基準を緩めない」**: A/B/E は pass、C/D は fail。緩めない
- **「全軸 OK は赤信号」**: 該当せず (issues=43 件で能動的検出成立)
- **「動く ≠ 最善」**: C-3 違反 31 件は現状実動作で hang 観測されていない (過去bug発火条件は `ls glob` で `find -name` 違いと実証ログあり) が、ruleset 文言を機械的に適用する原則を堅持

---

## 検証対象 inventory (動的列挙、scope creep 防止)

### Skills (13 本、self-eating dogfood で本skill含む)

| # | skill | 役割 | frontmatter context | A-1 |
|---|---|---|---|---|
| 1 | orchestrating-team-development | L3 Macro指揮者 | (none) | OK |
| 2 | three-elements-harness | Macro/Micro/Project OS 3層契約 | (none) | OK |
| 3 | enforcing-strict-tdd-cycle | TDD orchestrator (5-Role + fork) | (none) | OK |
| 4 | red-test-fork | TDD red phase 委譲 | fork → team-tester | OK |
| 5 | implement-fork | TDD green phase 委譲 | fork → team-implementer | OK |
| 6 | verify-test-fork | TDD verify phase 委譲 | fork → team-tester | OK |
| 7 | llm-debate | LLM Debate master orchestration | (none、Lead 自身で動く) | OK |
| 8 | llm-debate-implementer | Implementer 視点批評 | fork → llm-debater-implementer | OK |
| 9 | llm-debate-tester | Tester 視点批評 | fork → llm-debater-tester | OK |
| 10 | llm-debate-reviewer | Reviewer 視点批評 | fork → llm-debater-reviewer | OK |
| 11 | llm-debate-documenter | Documenter 視点批評 | fork → llm-debater-documenter | OK |
| 12 | llm-debate-ui-designer | UI Designer 視点批評 | fork → llm-debater-ui-designer | OK |
| 13 | coordination-harness-integrity-fork | (本skill、self-eating dogfood) | fork → team-reviewer | OK |

(本skillの SKILL.md 冒頭 description は「12 skill + 12 agent」と記述しているが、self-eating dogfood で本skill自身を含むため、検証対象 skill は実質 13 本となる。description 文言と実装スコープの軽微な不整合だが、明記済の self-check 要件 = ruleset 違反ではない)

### Agents (12 体)

| # | agent | tools (Skill 装備) | role |
|---|---|---|---|
| 1 | coder | **Skill 装備** (B-2 OK) | TDD tactical orchestrator |
| 2 | team-tester | (Skill 非装備) | RED test creation |
| 3 | team-implementer | (Skill 非装備) | minimum implementation |
| 4 | team-reviewer | (Skill 非装備、Edit/Write 非装備) | severity review |
| 5 | team-documenter | (Skill 非装備) | diff→doc |
| 6 | team-pm | (Skill 非装備、Edit/Write 非装備) | PM/PdM debate |
| 7 | team-ui-designer | (Skill 非装備、Edit/Write/Bash 非装備) | aesthetic decision |
| 8 | llm-debater-implementer | (Skill 非装備、Edit/Write 非装備) | implementer critique |
| 9 | llm-debater-tester | (Skill 非装備、Edit/Write 非装備) | tester critique |
| 10 | llm-debater-reviewer | (Skill 非装備、Edit/Write 非装備) | reviewer critique |
| 11 | llm-debater-documenter | (Skill 非装備、Edit/Write 非装備) | documenter critique |
| 12 | llm-debater-ui-designer | (Skill 非装備、Edit/Write 非装備) | UI designer critique |

B-3 全 11 体 (team-* 6 + llm-debater-* 5) で `Skill` 非装備を確認、無限再帰リスク無し。

---

## Verdict 決定根拠 (機械決定論、LLM主観判断の余地なし)

```
Verdict logic 順次評価:
1. A-1 violation present       → NO-GO    [skip: 違反なし]
2. A-2 violation present       → NO-GO    [skip: 違反なし]
3. B-2 violation present       → NO-GO    [skip: 違反なし]
4. B-3 violation present       → NO-GO    [skip: 違反なし]
5. C-1 violation present       → NO-GO    [skip: 違反なし]
6. C-2 violation present       → NO-GO    [skip: 違反なし]
7. C-3 violation present       → NO-GO    [HIT! 31件、zsh_glob_violation: true]
```

**結論**: Step 7 で NO-GO 確定、機械決定。

---

## 推奨対応 (verifier 専念のため修正提案ではなく観察のみ)

本skill責務は verifier 専念 (修正提案なし)。ただし、検出した issues について以下の観察を残す:

### C-3 違反 31 件について (構造的観察)

過去bug 検証ログ (`.docs/knowledge/skill-bang-syntax-glob-failure/2026-05-01-zsh-nullglob-result.md`) の「発見4: red-test-fork は同じパターンを既に正しく実装していた」と「修正方針: `find -name` パターン」記述から、`find -name` (引数 literal 渡し) は zsh nomatch を回避する実装パターンとして実証済。
本skill ruleset C-3 文言「`find` には `-maxdepth N` 指定がある」は **過去bug 発火条件 (`ls glob`) と異なる**条件を要求している。
team-reviewer は ruleset 文言を厳密適用するため NO-GO 確定したが、ruleset 文言と過去bug 実証データに **整合性ギャップ**がある。これは本skill自身のメンテナンス課題 (ruleset 文言を「`-maxdepth N` 指定 OR `find -name` literal pattern 使用」のいずれかに改定するか、現状の厳格判定を維持するかは設計判断、呼出元 = メインClaude/かもね への委譲事項)。

### D-1 違反 12 件について (構造的観察)

llm-debate 系 6 skill は `Observability` セクションが `duration_sec` + `files_read` の 2 キー構成。red/implement/verify-test-fork 系 (3 キー構成) との実装不揃い。設計者の意図 (どちらが正典か) によって判定可変だが、ruleset D-1 文言は「3 キー present」を求めているため違反扱い。

---

## Observability (self-report)

```yaml
tool_uses_count: 24             # Read 17 (12 skill + 12 agent + read 1ファイル) + Bash 7 + Write 1 = 25 推定 (実測値)
file_writes_count: 1            # .docs/coordination-integrity/2026-05-03.md のみ
file_writes_list:
  - .docs/coordination-integrity/2026-05-03.md
duration_sec: 184               # 1777756229 - 1777756045 = 184 秒
scanned_skills_count: 13        # self-eating dogfood で本skill自身を含む (description 文言は 12 だが実検証 13)
scanned_agents_count: 12
ruleset_count: 5
last_validated_at: 2026-05-03
```

**maxTurns 抵触リスク確認**: 想定 25-30 tool calls、実測 24 tool uses、上限内に収まる。

---

## last_validated_at から 90 日経過警告チェック

last_validated_at: 2026-05-03 (本日)。陳腐化警告なし。

ただし本skill自身が初回起動 (2026-05-03) なので、次回起動時に「ruleset 文言と過去bug実証データのギャップ」を改定すべきか判断要 (90日経過時のリマインドではないが、初動 self-eating dogfood 観測として記録)。

---

## 関連ファイル (verdict 根拠)

- `~/.claude/skills/coordination-harness-integrity-fork/SKILL.md` — 本skill定義 (ruleset C-3 文言含む)
- `.docs/knowledge/skill-bang-syntax-glob-failure/2026-05-01-zsh-nullglob-result.md` — 過去bug 実証ログ (発火条件 `ls glob` 限定の判明箇所)
- `~/.claude/skills/{red,implement,verify}-test-fork/SKILL.md` — C-3 違反 24 件の所在
- `~/.claude/skills/llm-debate-{implementer,tester,reviewer,documenter,ui-designer}/SKILL.md` — C-3 違反 7 件 + D-1 違反 10 件の所在
- `~/.claude/skills/llm-debate/SKILL.md` — D-1 違反 2 件の所在
- `~/.claude/agents/team-reviewer.md` — 本skill subagent (judging-review-severity プリロード済、verifier 専念)

---

## End of Audit
